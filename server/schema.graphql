input UserCredentials {
    login: String!
    password: String!
}

type AuthToken {
    token: String!
    userName: String!
}

type Author {
    id: ID!
    firstName: String!
    lastName: String!
}

type Book {
    id: ID!
    title: String!
    description: String!
    author: Author!
    comments: [Comment]!
}

type Comment {
    id: ID!
    author: String!
    text: String!
    createdAt: String!
}

type PageInfo {
    totalPages: Int!
    totalCount: Int!
    currentPage: Int!
}

type CommentsPagination {
    pageInfo: PageInfo!
    comments: [Comment]!
}

type Query {
    login(login: String!, password: String!): AuthToken!
    getAllBooks: [Book]!
    getAllAuthors: [Author]!
    getBook(id: ID!): Book!
    getComments(entityId: ID!, limit: Int!, offset: Int!): CommentsPagination!
}